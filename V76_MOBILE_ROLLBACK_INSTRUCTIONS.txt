Wild Rift Guide 手機回朔 v76 專用包

用途：
- 將 v77 曾修改的網站檔案，全部還原成 v76 第二階段鎖定版。
- 不需要重新上傳整個網站。

手機操作：
1. 下載並解壓縮本 ZIP。
2. 打開解壓後的 WildRiftGuide 資料夾。
3. 依照原本手機更新包的方式，將裡面的檔案上傳到 GitHub 專案相同路徑。
4. 遇到同名檔案時，全部選擇覆蓋／取代。
5. 保留資料夾結構，不要把 assets、pages 內的檔案移到根目錄。
6. 完成後 Commit。

建議 Commit：
revert: 回朔至v76第二階段攻略內容鎖定版

注意：
- 回朔後會暫時恢復 v76 當時的英雄名稱資料，因此「符文法師」也會先回來。
- 下一個重做的 v77 會以 v76 為基底，只額外把名稱改為「雷茲」，不再動到已鎖定攻略資料。
- V77_STAGE3_SITE_FUNCTIONS_LOCK_UPDATE.txt 即使留在根目錄也不影響網站；可以之後再刪除。
